# Brute Force Incident Response Playbook

## Purpose

This playbook documents how to detect, triage, contain, eradicate, recover from, and document a suspected brute force authentication attack using the Elastic SIEM lab.

## Scenario

An attacker attempts repeated SSH or Windows login attempts against a monitored endpoint. The SIEM detects multiple failed authentication events from the same source IP address or against the same user account.

## Detection Sources

- Linux authentication logs
- Windows Security Event Logs
- Elastic Agent data
- Kibana Discover
- Kibana Security alerts
- Failed login dashboards

## Detection Indicators

- Multiple failed logins from one source IP
- Repeated attempts against one username
- Failed logins followed by a successful login
- Login attempts outside normal lab activity
- High volume of authentication failures in a short time window

## Example Kibana Searches

```text
message: "Failed password"
```

```text
event.action: "logon-failed"
```

```text
winlog.event_id: 4625
```

```text
host.os.type: "windows" AND event.code: 4625
```

```text
host.os.type: "windows" AND event.code: 4624
```

```text
user.name: root OR user.name: administrator
```

## Triage Steps

1. Identify the affected host.
2. Identify the source IP address.
3. Determine which user account was targeted.
4. Count the number of failed attempts.
5. Check whether any successful login occurred after the failures.
6. Review related network or process activity.
7. Capture screenshots from Kibana as evidence.

## Containment Steps

- Disable or lock the targeted account if compromise is suspected.
- Block the attacking source IP at the firewall or host level.
- Restrict SSH or RDP access to trusted systems only.
- Require strong passwords or key-based authentication.
- Increase monitoring on the affected host.

## Eradication Steps

- Remove unauthorized accounts if discovered.
- Reset affected account passwords.
- Review privileged group membership.
- Remove persistence mechanisms if found.
- Patch exposed services if outdated.

## Recovery Steps

- Re-enable accounts only after validation.
- Confirm legitimate users can authenticate.
- Verify Elastic Agent is still reporting logs.
- Confirm no continued failed login activity exists.
- Document lessons learned.

## Evidence to Capture

- Failed login events in Kibana
- Successful login events after failed attempts
- Source IP address
- Target username
- Timeline of activity
- Screenshots of dashboard panels or Discover results

## Lab Evidence Collected

During Phase 2 testing, Windows authentication events were generated and reviewed in Kibana Discover.

The following evidence files support this playbook:

| Evidence File | Description |
|---|---|
| [phase-2-windows-failed-logon-4625-discover.png](../screenshots/phase-2-windows-failed-logon-4625-discover.png) | Shows failed Windows logon Event ID 4625 generated by wrong password attempts |
| [phase-2-windows-successful-logon-4624-discover.png](../screenshots/phase-2-windows-successful-logon-4624-discover.png) | Shows successful Windows logon Event ID 4624 generated by a valid login |
| [windows-agent-powershell-healthy.png](../screenshots/windows-agent-powershell-healthy.png) | Shows Elastic Agent running and connected on the Windows endpoint |
| [fleet-windows-agent-healthy.png](../screenshots/fleet-windows-agent-healthy.png) | Shows the Windows endpoint enrolled and healthy in Fleet |

### Failed Windows Logon Evidence

![Windows failed logon Event ID 4625](../screenshots/phase-2-windows-failed-logon-4625-discover.png)

The failed logon evidence confirms that the SIEM can detect authentication failures from the Windows Enterprise VM.

### Successful Windows Logon Evidence

![Windows successful logon Event ID 4624](../screenshots/phase-2-windows-successful-logon-4624-discover.png)

The successful logon evidence confirms that the SIEM can detect valid authentication activity. This is useful during brute force investigations because analysts should determine whether failed attempts were followed by a successful login.

## Investigation Notes

A brute force investigation should not stop at failed logons. Analysts should also search for successful logons after the failed attempts.

Important questions:

1. Were there repeated failed authentication attempts?
2. Was the same account targeted repeatedly?
3. Did a successful logon occur after the failures?
4. Was the successful logon expected or suspicious?
5. Did the account perform privileged activity after logging in?

## CISSP Domain Mapping

| CISSP Domain | Relevance |
|---|---|
| Domain 1: Security and Risk Management | Risk identification and response documentation |
| Domain 5: Identity and Access Management | Authentication monitoring and account protection |
| Domain 6: Security Assessment and Testing | Validation of detection capability |
| Domain 7: Security Operations | Incident detection, triage, containment, and evidence collection |

## Lessons Learned

This activity demonstrates how SIEM monitoring supports authentication security, access control, incident response, and operational security decision-making.

The combination of failed logon evidence and successful logon evidence provides a stronger investigation workflow because it allows the analyst to determine whether suspicious authentication attempts resulted in actual access.
